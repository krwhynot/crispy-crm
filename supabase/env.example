# Local Supabase Environment Variables
# Copy to supabase/.env and fill in values.
# Generate encryption key: openssl rand -base64 32
SUPABASE_AUTH_ENCRYPTION_KEY=<generate-with-openssl-rand-base64-32>
