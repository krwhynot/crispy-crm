# See https://help.github.com/ignore-files/ for more about ignoring files.

# editors
.idea/
*.iml
.vscode/

# dependencies
/node_modules

# testing
/coverage
/coverage-temp
/test-results

# production
/build

# misc
.DS_Store
.eslintcache

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
*.lnk
$RECYCLE.BIN/

# Environment files - Phase 1 Security Remediation
# Keep these files (committed):
# .env.example - Template showing structure
# .env.local - Local development config (safe Docker keys)

# Ignore these:
.env                        # Active configuration (copied from .env.local or .env.cloud)
.env.*                      # Catch-all: .env.production, .env.staging, .env.development, .env.test, etc.
.env.cloud                  # Cloud config with API keys (not committed)
!.env.example               # Exception: Keep .env.example (template)
!.env.local                 # Exception: Keep .env.local (safe Docker keys)
.env-archive/               # Archived old env files

npm-debug.log*
yarn-debug.log*
yarn-error.log*
supabase/.branches
supabase/.temp
supabase/functions/.env

dist/
/lib/
*storybook.log

# MCP Configuration (contains API keys)
.mcp.json

# Claude memory database (local to each developer)
*.db
*.db-journal
*.db-wal

# But DO track commands and config
!.claude/commands/

storybook-static

# Debug scripts (may contain credentials)
debug-*.js
debug-*.ts
troubleshoot-*.cjs
troubleshoot-*.js

# Screenshot artifacts (debugging)
*.png
!public/**/*.png
!docs/**/*.png

# Temporary/one-time documentation
*-ANALYSIS.md
*-GUIDE.md
*-SUMMARY.md
*_TEST.md

# Backup and sync operation files
backups/
*.sql.backup
logs/sync-operations/
scripts/dev/test-fetch.mjs

# Data files (binary files, spreadsheets, etc.)
data/

# Supabase seed files with real user data
supabase/cloud_users.sql
.vercel

# Git worktrees (isolated development workspaces)
.worktrees/

# Claude Code session files
.claude/checkpoint-state
.claude/memory-capture.log
.env.test
.env.cloud

# Drawbridge annotation files (visual UI feedback tool)
.moat/
moat-tasks.md
moat-tasks-detail.json
screenshots/

# Serena language server cache
.serena/cache/
.serena/memories/

# Claude Code session caches (keep commands/ and skills/)
.claude/tsc-cache/
.claude/agents/
.claude/hooks/
.claude/troubleshooting/
.claude/state/manifest.json

# Old Docker-based code intelligence (superseded by embedded approach)
.claude/qdrant/
.claude/ollama/

# Code intelligence index files (regenerate with `just discover-scip`)
.claude/state/index.scip
.claude/state/vectors.lance/
