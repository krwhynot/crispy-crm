# See https://help.github.com/ignore-files/ for more about ignoring files.

# editors
.idea/
*.iml
.vscode/

# dependencies
/node_modules

# testing
/coverage

# production
/build

# misc
.DS_Store
.eslintcache

# Environment files
# Keep these files (committed):
# .env.example - Template showing structure
# .env.local - Local development config (safe Docker keys)
# .env.cloud - Cloud config template (public anon key only)

# Ignore these:
.env                        # Active configuration (copied from .env.local or .env.cloud)
.env-archive/              # Archived old env files
.env.production            # Production secrets (if any)
.env.*.local              # Any local overrides
.env.staging                # Staging environment credentials

npm-debug.log*
yarn-debug.log*
yarn-error.log*
supabase/.branches
supabase/.temp

dist/
/lib/
.env
*storybook.log

# MCP Configuration (contains API keys)
.mcp.json

# Claude memory database (local to each developer)
*.db
*.db-journal
*.db-wal

# But DO track commands and config
!.claude/commands/

storybook-static

# Debug scripts (may contain credentials)
debug-*.js
debug-*.ts

# Backup and sync operation files
backups/
*.sql.backup
logs/sync-operations/
scripts/dev/test-fetch.mjs

# Supabase seed files with real user data
supabase/cloud_users.sql
.vercel
