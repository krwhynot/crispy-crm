# See https://help.github.com/ignore-files/ for more about ignoring files.

# editors
.idea/
*.iml
.vscode/

# dependencies
/node_modules

# testing
/coverage

# production
/build

# misc
.DS_Store
.eslintcache

# Environment files
# .env.local - COMMITTED (contains safe Docker demo keys for local development)
.env.production              # Actual production secrets (NEVER commit)
.env.development.local       # Local development overrides
.env.test.local             # Local test overrides
.env.production.local       # Local production overrides
# .env.production.example - COMMITTED (reference template for deployment)
.env.migration              # Migration-specific credentials
.env.staging                # Staging environment credentials

npm-debug.log*
yarn-debug.log*
yarn-error.log*
supabase/.branches
supabase/.temp

dist/
/lib/
.env
*storybook.log

# MCP Configuration (contains API keys)
.mcp.json

# Claude memory database (local to each developer)
*.db
*.db-journal
*.db-wal

# But DO track commands and config
!.claude/commands/

storybook-static

# Debug scripts (may contain credentials)
debug-*.js
debug-*.ts

# Backup and sync operation files
backups/
*.sql.backup
logs/sync-operations/
scripts/dev/test-fetch.mjs

# Supabase seed files with real user data
supabase/cloud_users.sql
